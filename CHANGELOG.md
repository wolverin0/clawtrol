# Changelog

All notable changes to ClawTrol are documented here.
Generated automatically from [conventional commits](https://www.conventionalcommits.org/).

## [Unreleased]

### 🚀 Features

- add passive Hermes mirror contract (`6b20885`)
- **ops:** operational hardening pass (`9444088`)
- **ops:** add Sentry error tracking (no-op without SENTRY_DSN) (`e1af80e`)
- **nightshift:** enforce budget gate on mission create / approve / launch / arm (`eb79319`)
- webhook idempotency + centralize external-service URLs (`665faa9`)
- **billing:** per-user LLM budget gate on agent-spawn endpoints (`f12dd5d`)
- add openclaw sync integrations (`6f1c984`)
### 🐛 Bug Fixes

- make hosted release gates deterministic (`e99beea`)
- enforce LF for executable scripts (`bb67554`)
- pass application URL to restore gate (`2bb66a3`)
- preserve host database connectivity (`3250474`)
- enforce immutable release security gates (`aeef777`)
- contain legacy execution and inert html (`ab1ba67`)
- drop nonexistent status column from TaskRun.create + integrity expectation (`2bd2b2b`)
- **db:integrity:** treat orphan schema_migrations rows as warning, not abort (`a3b4fc5`)
- **deps,n+1:** patch CVEs + eager-load Task associations on bulk delete (`f2475be`)
- enforce scope check + restore Agent Output description block (`4cdac3d`)
- order zombie notify before demote + loosen wake response check (`c695c59`)
- **routes:** add missing /api/v1/hooks/zeroclaw_auditor route (`b6d8e95`)
- schema regen + skip codemap events + analytics env override (`620a26c`)
- **db:** restore missing pipeline_* columns on tasks (`697efe9`)
- **encryption:** read AR::Encryption keys from ENV + rescue Configuration error (`c7a936a`)
- **auth:** session cookie secure flag follows request.ssl? not Rails.env (`94c3f3b`)
- **docker:** expose postgres on host port 15432 for non-container Rails (`3dd34af`)
- **layouts:** wire Tailwind stylesheet tag in application+auth layouts (`588396e`)
- disable SSL for local network, guard transcript_watcher listen gem (`1f78760`)
### 📚 Documentation

- update CHANGELOG.md [skip ci] (`1994ac7`)
- record safe-lan verification and battle test (`9bcacd5`)
- establish safe-lan remediation governance (`27e6999`)
- checkpoint battle-test diagnosis (`74b3177`)
- update CHANGELOG.md [skip ci] (`81ee995`)
- update CHANGELOG.md [skip ci] (`1e7bde1`)
- update CHANGELOG.md [skip ci] (`0c42334`)
- update CHANGELOG.md [skip ci] (`da85633`)
- update CHANGELOG.md [skip ci] (`f10cea6`)
- update CHANGELOG.md [skip ci] (`b2a53ed`)
- update CHANGELOG.md [skip ci] (`7ec20d9`)
- update CHANGELOG.md [skip ci] (`cb29ab7`)
- update CHANGELOG.md [skip ci] (`2b79a19`)
- add backup & restore runbook with verified procedure (`01b46d2`)
- update CHANGELOG.md [skip ci] (`8ffd925`)
- update CHANGELOG.md [skip ci] (`ddaa6f7`)
- update CHANGELOG.md [skip ci] (`2231414`)
- update CHANGELOG.md [skip ci] (`396f99c`)
- update CHANGELOG.md [skip ci] (`908c3e1`)
- update CHANGELOG.md [skip ci] (`fc95a11`)
- **audit:** add remediation roadmap alongside the audit report (`8aec26f`)
- **audit:** vibe-code-audit v2.1 + codebase maps (2026-04-17) (`2a26fef`)
- update CHANGELOG.md [skip ci] (`1f5a94b`)
- update CHANGELOG.md [skip ci] (`86f6484`)
- update CHANGELOG.md [skip ci] (`8c32ad4`)
### ♻️  Refactoring

- rename module ClawDeck → Clawtrol in config/application.rb (`40a6252`)
- **api:** PipelineController inherits BaseController (`f45d231`)
### ⚡ Performance

- **queue:** move SolidQueue out of Puma into its own systemd service (`b5573c8`)
### 🧪 Tests

- make zerobitch + git-diff tests run in clean environments (`3754c08`)
### 👷 CI

- brakeman --no-exit-on-error to ignore ERB parser quirks (`af180c0`)
### 💄 Style

- rubocop --autocorrect cleanup (94 offenses) (`6acb2b0`)
### 📦 Build

### 🔧 Chores

- **security:** harden revival secret handling (`ece72bb`)
- **recovery:** baseline live ClawTrol source (`4808269`)
- **brakeman:** triage 4 weak warnings as false positives (`484331d`)
- **claude:** install audit + guardrail kits in project .claude/ (`1bb704a`)
- **db:** regenerate schema.rb after audit-era migrations (`910c174`)
- **claude:** curate .claude/ rules + skills for local-first workflow (#31) (`409d6ac`)
- **cleanup:** remove dead audit-era infra + correct AUDIT topology note (`c31395c`)
- pin .ruby-version to 3.3.8 to match Dockerfile and rbenv installed version (`f7406fd`)
- **cleanup:** remove unused gems, fix Ruby version, tighten rack_attack, drop orphan credentials (`05dd191`)

## [v2026.3.2] — 2026-03-23

### 🚀 Features

- add gpt5.4mini/nano models, fix recurrence scope, fix hooks test (`b393556`)
- Phase 4 — mobile responsiveness + accessibility improvements (`f0adb6d`)
- Phase 3 — memory editor controller + 3 new themes (Synthwave, Nord, Dracula) (`7c08c37`)
- Phase 2 — ActionCable TaskUpdatesChannel + Chartkick analytics charts (`4bfa8e3`)
- Phase 1 — fix hardcoded colors, keyboard help modal, empty states, loading frames, toast notifications (`ea79beb`)
- PinchChat-inspired UI — tool badges, thinking blocks, token bar, split view (`dac86b9`)
- Zombie Reaper + reliable wake webhook (`7de0f0b`)
- show roadmap progress in board header (#692) (`b9371db`)
### 🐛 Bug Fixes

- auto-fill origin_chat_id from user.telegram_chat_id when not provided (`cd17ca2`)
- post-audit #858 — task.title NoMethodError, update! without rescue, nil guards (`455bebf`)
- nil guard for tasks in board column views (`8ff4cac`)
- chartkick importmap pin to CDN (was missing local asset) (`2709655`)
- 4 bugs — auditor FAIL_REWORK up_next, shell_out timeout, gemini_cli timeout, transcript OOM guard (`e06f329`)
- 4 bugs — recurring job atomic tx, fire_openclaw after_commit, log_event seq retry, kanban broadcast on reorder (`0f535e7`)
- 4 bugs — after_update to after_commit, validation runner up_next on fail, pipeline User.first, AgentTranscript OOM guard (`12741cd`)
- 4 bugs — bulk update_all callbacks, spawn_ready race, rate-limit false positive, dead spawn_ready in concern (`62d4b88`)
- 4 bugs — nightshift env var, handoff lease, factory wake loop, review followup auto-queue (`8caf830`)
- 4 bugs — recurring archived leak, zombie agent_claimed_at, garbled emoji, minimax quota (`6baf8de`)
- context menu renders view template with turbo-frame wrapper (#692) (`236d47d`)
- add timeout to context menu turbo frame loading (#692) (`43e7719`)
- roadmap modal uses showModal() for proper top-layer stacking (#692) (`1a22e71`)
### 📚 Documentation

- update CHANGELOG.md [skip ci] (`4d2258c`)
- rewrite features section — ZeroBitch fleet, factory, nightshift, personas, roadmaps, swarm, saved links all properly described (`c23dd9a`)
- update CHANGELOG.md [skip ci] (`f3ae1a5`)
- clean README with collapsible sections, fix skill IP/name, remove internal files (`8fadd8e`)
- update CHANGELOG.md [skip ci] (`13ec09e`)
### ♻️  Refactoring

- remove dead pipeline code from Task, AutoRunner, WebhookService (`e14a74b`)
### ⚡ Performance

### 🧪 Tests

- add unit tests for BoardRoadmap checked_items and progress_summary (#692) (`84a8e36`)
- add system tests for roadmap modal, progress, and context menu (#692) (`93068df`)
### 👷 CI

### 💄 Style

### 📦 Build

### 🔧 Chores


## [v2026.3.1] — 2026-03-03

### 🚀 Features

- health gate before autopull + delivery backoff retry (closes #22) (`5d7699d`)
### 🐛 Bug Fixes

### 📚 Documentation

- update CHANGELOG.md [skip ci] (`5787a44`)
### ♻️  Refactoring

### ⚡ Performance

### 🧪 Tests

### 👷 CI

### 💄 Style

### 📦 Build

### 🔧 Chores


## [v2026.3.0] — 2026-03-03

### 🚀 Features

- ship learning effectiveness, board file refs, zeroclaw auditor, sessions explorer (`67b8f98`)
- [factory] architecture add RoadmapExecutorSync to sync markdown to board tasks (`57e1de2`)
- expose effective_prompt in task API for orchestrator consumption (`7a6936a`)
- inject agent_persona context into effective_prompt (`b3c6ff5`)
- **p0:** description separation + structured TaskRun output + modal history tab (`92f5b36`)
- [factory] add Mission Control Health Dashboard (`7601016`)
- **architecture:** [factory] add health endpoint for mission control dashboard (`35f5dc9`)
- [factory] add dead route scanner rake task (`01be26e`)
- [factory] Daily Executive Digest (Telegram summary) (`ea20a7a`)
- **queue:** add board-fair scheduler, quotas, same-task follow-up, and runner summaries (`f8c58eb`)
- **navbar:** add Docs Hub link to desktop and mobile navigation (`e33b97d`)
- **agent-log:** durable sidecar ingest, backfill, and persisted timeline (`846e25a`)
- ship codemap monitor + durable agent activity telemetry release (`8aa59b4`)
- zerobitch observability + viewer download (#225) (`08b0f8b`)
- add Learning Inbox for self-audit proposals (#215) (`186c2a8`)
- replace icon-only nav with grouped dropdown categories (`9a196fb`)
- board roadmap v1 task generation (#142) (`6f6429b`)
- add run_lobster, resume_lobster, spawn_via_gateway actions + routes + UI (#127) (`c0d3139`)
- add LobsterRunner service + deploy-check pipeline (#127) (`a46f827`)
- **webhooks:** add agent_done hook endpoint for Factory cron webhook delivery (Item 3) (`acd29aa`)
- **lobster:** add LobsterRunner service, run/resume controller actions and routes (Item 1) (`c0e58a3`)
- **lobster:** add resume_token migration and pipeline files (Lobster integration step 1) (`5441ae3`)
- openclaw-only orchestration + pipeline stage consistency + dynamic model catalog + outcome reporting (`7158e76`)
- **factory:** cherry-pick GitHub integration — clone repos + create PRs (#95) (`80c9d6c`)
### 🐛 Bug Fixes

- [factory] skip self-dependency on missing ids (`6d6881b`)
- [factory] block expired api tokens (`e7316c2`)
- [factory] bound playground git diff range for shallow history (`f1a784c`)
- [factory] reject non-relative routes in dead route scanner (`453b7fb`)
- [factory] mark turbo-frame mismatch responses as failed (`e3d5048`)
- [factory] include root path in dead route scanner (`d77ea0a`)
- [factory] scope mission control snapshot cache by env (`e91cd77`)
- [factory] reject blank promotion gate repo_path (`d3c1373`)
- [factory] security restrict promotion gate to git repositories (`ca1adca`)
- [factory] harden dead route scanner nil-path handling (`91da2d9`)
- [factory] security cap cherry-pick batch size and add guard tests (`eb3f204`)
- **routing:** smart origin_thread_id + README P0 contract + doc unification (`fbe1e62`)
- [factory] harden mission control no-cache headers (`be622ed`)
- [factory] tighten GET verb matching in dead route scanner (`db29c92`)
- [factory] support roadmap checkbox list variants (`0a526d1`)
- [factory] fail closed for mission control shell metrics (`1b53e9b`)
- **transcript:** skip scope check for explicitly linked sessions (`caa7b59`)
- **p0:** resolve 6 workflow inconsistencies from audit (`3143779`)
- **hooks:** resolve session_id linking + transcript auto-discovery (`7b78ef2`)
- **p0:** enforce data contract — remove all description write paths (`b7fbea7`)
- [factory] security bound workflow expression length (`4ada937`)
- [factory] prevent cycle number race in factory runner v2 (`7742c34`)
- [factory] desktop navbar active states (`d486699`)
- [factory] disable caching for mission control dashboard (`8ef5fa9`)
- [factory] security harden promotion gate repo path (`3bb888c`)
- [factory] include regex-serialized GET routes in dead route scanner (`7eb0d49`)
- [factory] Bug Fixes: remove unused user fallback in factory engine (`3432772`)
- [factory] security sanitize promotion gate error output (`c97fc8f`)
- [factory] sanitize dead route scanner exception output (`3118a0d`)
- [factory] flag empty 2xx pages in dead route scanner (`88c8bfb`)
- [factory] show unknown migrations when db disconnected (`b6ce968`)
- [factory] scan GET routes when route has multiple verbs (`c8ddf30`)
- [factory] require explicit opt-in for Telegram fallback (`5263b53`)
- [factory] enforce max file viewer edit payload size (`f44f850`)
- [factory] security harden cherry-pick commit filtering (`d210bc9`)
- [factory] escape HTML in daily digest telegram output (`95d5e6c`)
- **auto-runner:** add safe burst headroom and document runtime hardening release (`5c86e68`)
- **api:** enforce strict task-scoped agent_log session resolution (`826af08`)
- **zerobitch:** scope activity to real task sessions and remove codemap panel (`f30e326`)
- keep /view downloads working for large files (#225) (`b693c5a`)
- improve zerobitch fleet UX and view save (`0e65785`)
- restore zerobitch routes + add codemap_monitor route (both were uncommitted) (`5049453`)
- **codemap:** fix atlas sprite coordinates - replace 0,0,16,16 placeholders with real pixel coords (`0a4e298`)
- add turbo-frame=_top to all nav dropdown links, fix audits_path (`46f5aef`)
- precompile assets to include nav_dropdown_controller in importmap (`b771571`)
- remove invalid ( in link_to block calls in nav dropdowns (`7160618`)
- resolve merge conflict in nav_icons - keep grouped dropdown version (`b6798bf`)
- remove extra ) in link_to ERB calls in nav dropdowns (`0be2e85`)
- **ci:** fix all remaining test failures in clawtrol/main (`ac73169`)
- **ci:** fix remaining test failures (lint whitespace, title→name, validation assertions, skill scanner) (`60163ac`)
- **ci:** remove tests for non-existent FactoryCycleLog and SwarmIdea attributes (`8bc130c`)
- **ci:** accumulated test fixes, service improvements, nightshift scheduled_hour migration (`949a725`)
- **ci:** fix remaining unit/system test failures (`8edec0b`)
- **ci:** factory_cron_sync ENOENT rescue, invite_code remove stale attrs, swarm N+1, board/swarm system tests (`fccc024`)
- **ci:** remove tests for non-existent session attributes, fix zerobitch connect order (`1f9ff19`)
- **ci:** disable Bullet unused_eager_loading in CI, fix remaining rubocop lint in services/migrate (`0c8e0db`)
- **ci:** rubocop lint, test class conflict (AuditsControllerTest), preserve WIP in boards/tasks_controller (`bc0a65b`)
- update Bullet API and faraday CVE-2026-25765 (`8aee349`)
- resolve CI failures - bullet gem group, rubocop lint, brakeman warnings (`1929530`)
- **mobile:** bottom bar works on all pages, padding fix, Terminal graceful fallback (#120) (`59599b9`)
- **mobile:** add data-turbo-frame=_top to bottom nav links — fixes Content missing on tab switch (`1bc13ac`)
### 📚 Documentation

- update CHANGELOG to 2026.3.0 and README feature list (`a999ce3`)
- [factory] log dependency scan perf tweak (`891eca8`)
- [factory] log api token scope tweak (`8aed1b3`)
- [factory] log validation safety concern (`91773ce`)
- [factory] log task dependency validation guard (`2ae08b0`)
- [factory] log api token expiry guard (`474d12a`)
- [factory] log model limit timing tweak (`a7e5fad`)
- [factory] log validation prefix test (`d8d8183`)
- [factory] log code quality cleanup (`e2d416d`)
- [factory] log dead route scanner security hardening (`d447b08`)
- [factory] log dead route turbo-frame mismatch fix (`5e8e8c8`)
- [factory] log promotion gate fail-fast cycle (`df13936`)
- update CHANGELOG.md [skip ci] (`9fe5b78`)
- [factory] log security cache-header hardening cycle (`6b7f426`)
- [factory] log dead route scanner GET matching fix (`de5efe5`)
- update CHANGELOG.md [skip ci] (`304a18e`)
- update CHANGELOG.md [skip ci] (`b570fef`)
- update CHANGELOG.md [skip ci] (`2bb2cca`)
- update CHANGELOG.md [skip ci] (`df50665`)
- [factory] log workflow engine logger consistency cycle (`ceb3f0e`)
- update CHANGELOG.md [skip ci] (`4e23afa`)
- update README + onboarding + integration for P0 data contract and session linking (`7ac9a3a`)
- [factory] log security expression guard cycle (`9769a36`)
- [factory] log mission control cache hardening cycle (`4f3917d`)
- [factory] log empty-response scanner hardening (`7ec518d`)
- [factory] update improvement log with mission control health dashboard (`999b5b2`)
- update CHANGELOG.md [skip ci] (`d0191e5`)
- update CHANGELOG.md [skip ci] (`6712268`)
- update CHANGELOG.md [skip ci] (`5234b40`)
- update CHANGELOG.md [skip ci] (`37e544a`)
- update CHANGELOG.md [skip ci] (`a044e4e`)
- **report:** add 2026-02-20 big release commit/push report (`0e06cc6`)
- update CHANGELOG.md [skip ci] (`70dfb29`)
- update CHANGELOG.md [skip ci] (`e1f9c88`)
- add ZeroBitch cover image to README (`2c9454d`)
- update CHANGELOG.md [skip ci] (`9d41170`)
- update CHANGELOG.md [skip ci] (`81bde00`)
- update CHANGELOG.md [skip ci] (`45f38a7`)
- update CHANGELOG.md [skip ci] (`c68c4c7`)
- update CHANGELOG.md [skip ci] (`d741cff`)
- update CHANGELOG.md [skip ci] (`b978993`)
- update CHANGELOG.md [skip ci] (`27b024d`)
- update CHANGELOG.md [skip ci] (`3b3fb2a`)
- update CHANGELOG.md [skip ci] (`a3e8bf1`)
- update CHANGELOG.md [skip ci] (`2e78f03`)
- update CHANGELOG.md [skip ci] (`15d30a6`)
- add complete feature diagnostic with intent analysis for 30 features (`6954cf5`)
- update CHANGELOG.md [skip ci] (`c83354c`)
- **readme:** explain what ZeroBitch is — swarm orchestration for ZeroClaw instances (`c868e1e`)
- update CHANGELOG.md [skip ci] (`d36c341`)
### ♻️  Refactoring

- [factory] make api token by_user chainable (`b2d5392`)
- [factory] share validation command safety (`e991c1a`)
- [factory] architecture preload roadmap sync links and tasks (`222bfff`)
- [factory] remove duplicate strict_loading (`15a313b`)
- [factory] architecture refactor daily executive digest service scope (`409519c`)
- [factory] remove unused legacy nav_icons partial (`c2fa729`)
- [factory] extract dead route scan result builders (`ff4bceb`)
- [factory] use injected logger in workflow engine (`7c379ee`)
- [factory] unify last-board nav path resolution (`0728ce5`)
- [factory] code quality centralize promotion gate checks catalog (`3717bb5`)
- [factory] add promotion gate service for cherry-pick verify (`7ad87f6`)
- [factory] code quality: DRY default work branch fallback (`ff0083b`)
- [factory] architecture: extract mission control health snapshot service (`24988bd`)
- [factory] Unified Navbar Registry (desktop + mobile parity) (`1829c58`)
- [factory] extract BehavioralInterventionUpdaterService (`013a9bd`)
- remove pipeline_assist mode — openclaw_only is the sole orchestration path (`327e6af`)
### ⚡ Performance

- [factory] avoid shift in dependency scan (`c7844e3`)
- [factory] reuse time in model limit reset (`7226dff`)
- [factory] reduce digest query churn (`644ec6b`)
- [factory] fail-fast promotion gate checks (`89b714a`)
- [factory] dedupe sessions explorer task-link keys (`f92fb5d`)
- [factory] cache mission control health snapshot (`b725955`)
- [factory] dedupe dead route paths in single pass (`051f85f`)
- [factory] Performance: use Open3 chdir for promotion checks (`2bfea0b`)
- [factory] skip wildcard routes in dead route scanner (`f10d40a`)
### 🧪 Tests

- [factory] cover validation command prefix (`c3f83a0`)
- [factory] cover sub-agent output contract normalization (`a2be4a3`)
- [factory] cover git worktree marker in promotion gate (`44be431`)
- [factory] cover navigation helper active-state branches (`a1fa6e4`)
- [factory] add primary_navigation board-link regression tests (`6ed088e`)
- [factory] add mission control cache ttl regression (`8dc0304`)
- [factory] add promotion gate timeout/path regression tests (`e34eb0e`)
- [factory] Testing: add factory engine init fallback regression (`d2ca12b`)
- [factory] add ModelCatalogService coverage (`2ed9227`)
- [factory] cover dead route scanner behavior (`a42b7f7`)
- [factory] fix missing assertions in job and model tests (`eb2bdad`)
- stabilize board system login setup for deterministic selenium runs (`270da66`)
### 👷 CI

### 💄 Style

### 📦 Build

### 🔧 Chores

- merge factory/minimax-auto — Mission Control health dashboard, promotion gate, dead route scanner, API token expiry, roadmap sync (`b0d2525`)
- remove codemap feature, assets, and monitor endpoints (`6e3c8fd`)
- remove unused large binary artifacts from repo (`8d5a6aa`)

## [pre-audit-fixes] — 2026-04-17

### 🚀 Features

- add openclaw sync integrations (`6f1c984`)
- add gpt5.4mini/nano models, fix recurrence scope, fix hooks test (`b393556`)
- Phase 4 — mobile responsiveness + accessibility improvements (`f0adb6d`)
- Phase 3 — memory editor controller + 3 new themes (Synthwave, Nord, Dracula) (`7c08c37`)
- Phase 2 — ActionCable TaskUpdatesChannel + Chartkick analytics charts (`4bfa8e3`)
- Phase 1 — fix hardcoded colors, keyboard help modal, empty states, loading frames, toast notifications (`ea79beb`)
- PinchChat-inspired UI — tool badges, thinking blocks, token bar, split view (`dac86b9`)
- Zombie Reaper + reliable wake webhook (`7de0f0b`)
- show roadmap progress in board header (#692) (`b9371db`)
- health gate before autopull + delivery backoff retry (closes #22) (`5d7699d`)
- ship learning effectiveness, board file refs, zeroclaw auditor, sessions explorer (`67b8f98`)
- [factory] architecture add RoadmapExecutorSync to sync markdown to board tasks (`57e1de2`)
- expose effective_prompt in task API for orchestrator consumption (`7a6936a`)
- inject agent_persona context into effective_prompt (`b3c6ff5`)
- **p0:** description separation + structured TaskRun output + modal history tab (`92f5b36`)
- [factory] add Mission Control Health Dashboard (`7601016`)
- **architecture:** [factory] add health endpoint for mission control dashboard (`35f5dc9`)
- [factory] add dead route scanner rake task (`01be26e`)
- [factory] Daily Executive Digest (Telegram summary) (`ea20a7a`)
- **queue:** add board-fair scheduler, quotas, same-task follow-up, and runner summaries (`f8c58eb`)
- **navbar:** add Docs Hub link to desktop and mobile navigation (`e33b97d`)
- **agent-log:** durable sidecar ingest, backfill, and persisted timeline (`846e25a`)
- ship codemap monitor + durable agent activity telemetry release (`8aa59b4`)
- zerobitch observability + viewer download (#225) (`08b0f8b`)
- add Learning Inbox for self-audit proposals (#215) (`186c2a8`)
- replace icon-only nav with grouped dropdown categories (`9a196fb`)
- board roadmap v1 task generation (#142) (`6f6429b`)
- add run_lobster, resume_lobster, spawn_via_gateway actions + routes + UI (#127) (`c0d3139`)
- add LobsterRunner service + deploy-check pipeline (#127) (`a46f827`)
- **webhooks:** add agent_done hook endpoint for Factory cron webhook delivery (Item 3) (`acd29aa`)
- **lobster:** add LobsterRunner service, run/resume controller actions and routes (Item 1) (`c0e58a3`)
- **lobster:** add resume_token migration and pipeline files (Lobster integration step 1) (`5441ae3`)
- openclaw-only orchestration + pipeline stage consistency + dynamic model catalog + outcome reporting (`7158e76`)
- **factory:** cherry-pick GitHub integration — clone repos + create PRs (#95) (`80c9d6c`)
- **factory-v2:** connect play/pause/stop to OpenClaw cron sync (#87) (`d136d19`)
- migrate MiniMax cron to Factory v2 — cycle create endpoint + cron reporting (#88) (`0a4c21b`)
- **factory-v2:** workspace + github fields from Codex #94 (`9f8e713`)
- **factory:** add workspace/branch fields to UI + API controller (#94) (`f7c7d25`)
- **factory-v2:** add workspace_path + GitHub URL to loop creation UI (#94) (`93ac466`)
- **factory-v2:** add FactoryRunnerV2Job + StackDetector + PromptCompiler (#93) (`e1d2fa9`)
- **cronjobs:** add Delete button, remove stale antfarm crons, drop error-prone cache (`ff2482c`)
- **factory-v2:** add FactoryRunnerV2Job + StackDetector + PromptCompiler (#93) (`16a6630`)
- **factory:** add confidence scoring + finding dedup + auto-feed logic (#86) (`2985944`)
- **crons:** add edit functionality to cron builder UI (#90) (`f9cb4c2`)
- **factory:** add setup_workspace! + teardown_workspace! with git worktree + DB sandbox (#85) (`04f2018`)
- **factory:** expand UI with loop card details, agents tab, findings tab (#84) (`b14ef1f`)
- **cron-builder:** replace channel text input with delivery target dropdown (#89) (`a1f7ce5`)
- **factory:** seed 10 built-in factory agents from VoltAgent/Anthropic sources (`b0d3b3d`)
- **factory-v2:** add FactoryAgents + FactoryLoopAgents API controllers + tests (#82) (`9ff4c9c`)
- **factory-v2:** add Factory v2 models + associations + validations + tests (#81) (`7fab6ec`)
- **factory-v2:** add Factory v2 migrations — expand loops/cycle_logs + agents/runs/loop-agents tables (#80) (`e232b49`)
- **factory-v2:** add Factory v2 migrations — agents, runs, loop-agents tables + expand loops/cycle_logs (#80) (`b407e58`)
- integrate minimax playground main (`63f4aab`)
- **saved-links:** add deep summary toggle (#632) (`a88b318`)
- audit ingest API + auto-update interventions (#649) (`1fe24cd`)
- self-audit dashboard with trend charts + intervention tracker (#648) (`5a1f04d`)
- add audit_reports + behavioral_interventions tables (`b69bfff`)
- inline edit note with pencil icon (#645) (`d9febde`)
- wave 3 integration -- MiniMax factory 140 commits merged (`29798de`)
- **boards:** agent factory - auto-generate personas per board (`4b1e00b`)
- **tasks:** add deep research toggle for multi-agent analysis (#641) (`c42c365`)
- **swarm:** favorites, board routing, launch history, pipeline stepper + bugfixes (`54e5339`)
- **saved-links:** replace audio with deep summary toggle (#640) (`536d990`)
- webhook retry with exponential backoff (#635) (`5bf0cd4`)
- add APP_BASE_URL env variable — remove all hardcoded URLs (`7b2945e`)
- add APP_BASE_URL env variable — remove all hardcoded URLs (`ca77a5d`)
- audio_summary toggle in inline edit + TTS generation (#630) (`177238d`)
- **saved_links:** add audio_summary toggle + audio_file_path column (`0ecf31b`)
- inline edit note on saved links (`f384bd6`)
- rename saved_links.title to note + fix execution_plan crash (`507c6dc`)
- **factory-wave2:** integrate 37 MiniMax factory commits — features, security, tests (`e9c0a89`)
- fix SolidQueue adapter + rewrite AI onboarding in README (`d2f9320`)
- auto-review gate — evaluate agent output before in_review (#622) (`8209531`)
- auto-review gate — evaluate agent output before in_review (#622) (`ada007d`)
- default pipeline_enabled to true for all new tasks (#619) (`7d85932`)
- Swarm Launcher — curated task idea launcher with pipeline integration (#619) (`b4467b6`)
- **pipeline:** activate ClawRouter — observation mode off (#618) (`3c8f642`)
- **pipeline:** ClawRouter 3-layer pipeline -- triage, context compiler, router, advancer (`9d8e880`)
- agent_transcripts table — persist prompts/outputs to DB (#616) (`8ed7aac`)
- **features:** sprint 4 — invites, conditional workflows, notifications, search, cron CRUD (`2786e2d`)
- **integration:** sprint 3 — OpenClaw gateway health, costs, channels, crons, models sync (`8d70e88`)
- **workflows:** enhanced visual DAG editor with 8 node types and rich properties (`6f8a509`)
- add conventional commits hook, changelog generator, and CI workflow (`3dde1cb`)
- native task notifications (Telegram + webhook) (`c76cd7c`)
- add Nightshift execution backend (runner + engine + schema fix) (`194ef79`)
- factory loop system prompts seed task (#570) (`9c9543b`)
- factory cycle results webhook (#569) (`f06329f`)
- factory engine model callbacks (#568) (`ff38022`)
- factory engine schema + service + jobs (#567) (`c54ffd3`)
- agent factory module — DB, API, UI (`9d31468`)
- nightshift v2 — DB-backed missions with frequencies and auto-ticking (`d376118`)
- rebuild nightshift from hardcoded array to DB-backed missions (`c21bc9a`)
- **workflows:** add DAG workflow editor MVP (`79b2989`)
- **command:** show last message snippet from session jsonl (`8e940a6`)
- Add Agent Command Center at /command (`41e7f49`)
- nightshift mission #19 — Toolchain Version Checker (`7d9e6af`)
- Add task state_data and agent swarm view (`d278620`)
- /keys page for secure API key management (`55a5a24`)
- nightshift orchestrator - persistent selections via DB (`f0ab636`)
- auto-detect rate limits from agent findings + parse 'in ~N min' format (`a29c08b`)
- use fxtwitter API to fetch X/Twitter post content (`223b767`)
- process saved links with Gemini analysis (`f974da6`)
- add mobile hamburger menu with all nav links + add nightshift and saved_links to desktop nav (`ed37872`)
- add Link Inbox for nightshift URL processing (`5f77879`)
- convert nightshift selector from static HTML to dynamic Rails endpoint (`9360ba8`)
- add nightshift API endpoints for task selector (`b6d36e8`)
- visual diff viewer for agent output (#363) (`c45e2ff`)
- **marketing:** real image generation via gpt-image-1 in playground #450 (`767b308`)
- /marketing file browser endpoint for marketing docs (`2b878ce`)
- Add MGS codec sound effects Easter egg (#174) (`e576349`)
- OpenClaw integration wizard — 5-step guided setup (#138) (`2dcc748`)
- agent terminal improvements — smart filtering, progress timeline, search (#137) (`3aafa8c`)
- global search + filter bar — search across boards, filter by status/model/priority (#136) (`0f93c9a`)
- package ClawTrol as OpenClaw skill (#144) (`2005fbe`)
- auto-validation pipeline — agent_complete triggers background validation (#151) (`def50b0`)
- AI validation suggestion - auto-generate validation commands (#149) (`cae46b4`)
- task templates + slash commands - /review /bug /doc /test /research (#130) (`ea3e784`)
- notification system - bell icon, unread count, browser notifications (#127) (`cabb56f`)
- complete notification system with bell icon, API, and browser notifications (#127) (`ed4736d`)
- Docker Compose production setup + install.sh (#125) (`8a62fda`)
- system tests with Capybara + headless Chrome - 9 tests passing (#141) (`fe22b2b`)
- tabbed settings page - Profile/Agent/AI/Integration (#124) (`59309f6`)
- quick wins - done counter, timestamps, parent links, copy URL, confetti (#139) (`1a95295`)
- undo toast for status changes with 5s countdown (#128) (`d936100`)
- keyboard shortcuts - n=new task, Ctrl+/=terminal, ?=help modal (#143) (`994a3d5`)
- expand button for file viewer - fullscreen overlay modal (`11809a5`)
- file viewer in task modal + full-screen desktop modal (#120) (`9dd4aaf`)
- Fix archived view scroll + infinite pagination (`3c90931`)
- Add model rate limit tracking and auto-fallback system (`a7dbebf`)
- Multi-board support, auto-refresh, and UX improvements (`0df35b5`)
- add board tabs navbar with task counts and auto-routing (`46d9036`)
- add spawn_ready and link_session endpoints for automated agent tracking (`ab10ea6`)
- show parent output in follow-up modal, document session key requirement (`7e4432e`)
- Extend hover preview to in_review tasks (`524ca29`)
- Mobile terminal toggle + pin from modal (`7199960`)
- Mobile-friendly agent preview with bottom sheet modal (Task #32) (`5e593ff`)
- Always-visible action buttons + Real-time agent preview on hover (`ca2fb34`)
- Task ID display + Quick action buttons (NEXT, Follow-up) (`07ec84e`)
- Agent integration enhancements (`42d206b`)
- Agent live view, model routing, recurring tasks, and UI improvements (`a5b92da`)
- add onboarding board with starter tasks for new users (`a3b95e0`)
- add Discord community links (readme, dropdown, footer) (`c6506f6`)
### 🐛 Bug Fixes

- **layouts:** wire Tailwind stylesheet tag in application+auth layouts (`588396e`)
- disable SSL for local network, guard transcript_watcher listen gem (`1f78760`)
- auto-fill origin_chat_id from user.telegram_chat_id when not provided (`cd17ca2`)
- post-audit #858 — task.title NoMethodError, update! without rescue, nil guards (`455bebf`)
- nil guard for tasks in board column views (`8ff4cac`)
- chartkick importmap pin to CDN (was missing local asset) (`2709655`)
- 4 bugs — auditor FAIL_REWORK up_next, shell_out timeout, gemini_cli timeout, transcript OOM guard (`e06f329`)
- 4 bugs — recurring job atomic tx, fire_openclaw after_commit, log_event seq retry, kanban broadcast on reorder (`0f535e7`)
- 4 bugs — after_update to after_commit, validation runner up_next on fail, pipeline User.first, AgentTranscript OOM guard (`12741cd`)
- 4 bugs — bulk update_all callbacks, spawn_ready race, rate-limit false positive, dead spawn_ready in concern (`62d4b88`)
- 4 bugs — nightshift env var, handoff lease, factory wake loop, review followup auto-queue (`8caf830`)
- 4 bugs — recurring archived leak, zombie agent_claimed_at, garbled emoji, minimax quota (`6baf8de`)
- context menu renders view template with turbo-frame wrapper (#692) (`236d47d`)
- add timeout to context menu turbo frame loading (#692) (`43e7719`)
- roadmap modal uses showModal() for proper top-layer stacking (#692) (`1a22e71`)
- [factory] skip self-dependency on missing ids (`6d6881b`)
- [factory] block expired api tokens (`e7316c2`)
- [factory] bound playground git diff range for shallow history (`f1a784c`)
- [factory] reject non-relative routes in dead route scanner (`453b7fb`)
- [factory] mark turbo-frame mismatch responses as failed (`e3d5048`)
- [factory] include root path in dead route scanner (`d77ea0a`)
- [factory] scope mission control snapshot cache by env (`e91cd77`)
- [factory] reject blank promotion gate repo_path (`d3c1373`)
- [factory] security restrict promotion gate to git repositories (`ca1adca`)
- [factory] harden dead route scanner nil-path handling (`91da2d9`)
- [factory] security cap cherry-pick batch size and add guard tests (`eb3f204`)
- **routing:** smart origin_thread_id + README P0 contract + doc unification (`fbe1e62`)
- [factory] harden mission control no-cache headers (`be622ed`)
- [factory] tighten GET verb matching in dead route scanner (`db29c92`)
- [factory] support roadmap checkbox list variants (`0a526d1`)
- [factory] fail closed for mission control shell metrics (`1b53e9b`)
- **transcript:** skip scope check for explicitly linked sessions (`caa7b59`)
- **p0:** resolve 6 workflow inconsistencies from audit (`3143779`)
- **hooks:** resolve session_id linking + transcript auto-discovery (`7b78ef2`)
- **p0:** enforce data contract — remove all description write paths (`b7fbea7`)
- [factory] security bound workflow expression length (`4ada937`)
- [factory] prevent cycle number race in factory runner v2 (`7742c34`)
- [factory] desktop navbar active states (`d486699`)
- [factory] disable caching for mission control dashboard (`8ef5fa9`)
- [factory] security harden promotion gate repo path (`3bb888c`)
- [factory] include regex-serialized GET routes in dead route scanner (`7eb0d49`)
- [factory] Bug Fixes: remove unused user fallback in factory engine (`3432772`)
- [factory] security sanitize promotion gate error output (`c97fc8f`)
- [factory] sanitize dead route scanner exception output (`3118a0d`)
- [factory] flag empty 2xx pages in dead route scanner (`88c8bfb`)
- [factory] show unknown migrations when db disconnected (`b6ce968`)
- [factory] scan GET routes when route has multiple verbs (`c8ddf30`)
- [factory] require explicit opt-in for Telegram fallback (`5263b53`)
- [factory] enforce max file viewer edit payload size (`f44f850`)
- [factory] security harden cherry-pick commit filtering (`d210bc9`)
- [factory] escape HTML in daily digest telegram output (`95d5e6c`)
- **auto-runner:** add safe burst headroom and document runtime hardening release (`5c86e68`)
- **api:** enforce strict task-scoped agent_log session resolution (`826af08`)
- **zerobitch:** scope activity to real task sessions and remove codemap panel (`f30e326`)
- keep /view downloads working for large files (#225) (`b693c5a`)
- improve zerobitch fleet UX and view save (`0e65785`)
- restore zerobitch routes + add codemap_monitor route (both were uncommitted) (`5049453`)
- **codemap:** fix atlas sprite coordinates - replace 0,0,16,16 placeholders with real pixel coords (`0a4e298`)
- add turbo-frame=_top to all nav dropdown links, fix audits_path (`46f5aef`)
- precompile assets to include nav_dropdown_controller in importmap (`b771571`)
- remove invalid ( in link_to block calls in nav dropdowns (`7160618`)
- resolve merge conflict in nav_icons - keep grouped dropdown version (`b6798bf`)
- remove extra ) in link_to ERB calls in nav dropdowns (`0be2e85`)
- **ci:** fix all remaining test failures in clawtrol/main (`ac73169`)
- **ci:** fix remaining test failures (lint whitespace, title→name, validation assertions, skill scanner) (`60163ac`)
- **ci:** remove tests for non-existent FactoryCycleLog and SwarmIdea attributes (`8bc130c`)
- **ci:** accumulated test fixes, service improvements, nightshift scheduled_hour migration (`949a725`)
- **ci:** fix remaining unit/system test failures (`8edec0b`)
- **ci:** factory_cron_sync ENOENT rescue, invite_code remove stale attrs, swarm N+1, board/swarm system tests (`fccc024`)
- **ci:** remove tests for non-existent session attributes, fix zerobitch connect order (`1f9ff19`)
- **ci:** disable Bullet unused_eager_loading in CI, fix remaining rubocop lint in services/migrate (`0c8e0db`)
- **ci:** rubocop lint, test class conflict (AuditsControllerTest), preserve WIP in boards/tasks_controller (`bc0a65b`)
- update Bullet API and faraday CVE-2026-25765 (`8aee349`)
- resolve CI failures - bullet gem group, rubocop lint, brakeman warnings (`1929530`)
- **mobile:** bottom bar works on all pages, padding fix, Terminal graceful fallback (#120) (`59599b9`)
- **mobile:** add data-turbo-frame=_top to bottom nav links — fixes Content missing on tab switch (`1bc13ac`)
- **zerobitch:** 4 bugs — docker name matching, class vs instance methods, state field, path constants (`f1b5da5`)
- **zerobitch:** docker name matching — containers showed as stopped when running (`27d9168`)
- correct inverse_of on followup_task association (`c49eff3`)
- **factory:** use openclaw CLI instead of HTTP API for cron sync (`a339577`)
- **mobile:** use turbo-frame src instead of invalid Turbo.visit frame option (`de61fb2`)
- default origin_chat_id on task creation → results route to Mission Control instead of DM (`0fe9da6`)
- factory runner job tests — WebMock syntax + assertion fixes (#87) (`c08001f`)
- memory search health — use CLI probe instead of non-existent HTTP API (`229a2c3`)
- default DB name to clawdeck_development (was clawtrolplayground_development) (`495f026`)
- **mobile:** task modal scroll — left column + Agent Activity fully scrollable (`bca91ad`)
- **cronjobs:** E2E fixes — disabled jobs visible, Edit opens builder, CRUD via CLI (`0f532a2`)
- **cronjobs:** fix CLI gateway auth + create flow + model dropdown + Edit button (`880f92b`)
- **mobile:** task modal scroll - add bottom padding + fix flex for agent activity visibility (#84) (`b6ce525`)
- **config:** fall back to CLAWTROL_HOOKS_TOKEN env var for hooks auth (`deb22ad`)
- **saved-links:** restore inbox rendering and add QA validation baseline (`d6cc18e`)
- move cost snapshot strict_loading inside model class (`ccec767`)
- fail-safe encrypted user token reads to prevent board crash (`f60b729`)
- make missing user_id index migration idempotent (`1409ab2`)
- restore stable core contracts after minimax merge (`b44b839`)
- harden marketing path traversal check (`8bc9e5c`)
- align associations and marketing validation contracts (`98f5d1f`)
- restore stable session and task diff/activity contracts (`d776cd5`)
- remove duplicate AddTasksCountToAgentPersonas migration file (`b5bd15c`)
- unblock merged suite and harden edge cases (`014cef8`)
- stabilize merged model/controller drift and diff workspace path (`96f318c`)
- repair model strict_loading placement and task inverse associations (`7de0210`)
- align RunnerLease visibility tests and refresh schema (`eeccbc9`)
- **test:** remove duplicated RunnerLease test block from merge (`a24e92d`)
- downgrade migration version 8.2 to 8.1 (`23fcc10`)
- remove duplicated marketing controller tests from merge (`831346e`)
- rename duplicate migration to unique name (`3c6b5a1`)
- resolve duplicate migration class name for tasks_count (`6a1f973`)
- make duplicate tasks_count migration idempotent (`63fd03d`)
- make remaining imported column migrations idempotent (`6d79dce`)
- make imported index migrations re-runnable (`8595e46`)
- make auto runner partial index migration idempotent (`f945598`)
- make token usage indexes migration idempotent (`1cbb0cc`)
- rack-attack throttled_responder compatibility (`cfa43a3`)
- **pipeline:** advance unstarted tasks to routed via AutoRunner (`c4cb39d`)
- pipeline unstarted tasks become runnable (#697) (`1bb9958`)
- **tasks:** allow destroy even with agent_test_recordings association (#695) (`ff70ffa`)
- **telegram:** deliver task reports to origin topic (#693) (`5f6ea8d`)
- **pipeline/mobile:** in-modal pipeline viewer + touch scroll + contextmenu anchor (#691) (`6367236`)
- **pipeline/mobile:** dashboard + modal scroll/context menu (#691) (`d29bbaf`)
- **pipeline/mobile:** dashboard + modal scroll/context menu (#691) (`d514869`)
- **pipeline:** route planning tasks to codex (override glm) (#690) (`e40e9fd`)
- **mobile:** prevent task panel overlap and horizontal bleed (`e3e9e97`)
- **ui:** restore context menu on right-click and mobile long-press (`f38f530`)
- **api:** remove TaskSerializer references broken by wave3 merge (`1f71887`)
- add dependency_graph to set_board before_action + aggregator support (#639) (`6fb995f`)
- **api:** remove missing TaskSerializer reference + add unstarted to pipeline stages (`05dd067`)
- enable SolidQueue plugin unconditionally in Puma (`17bdcbb`)
- add pipeline_enabled to task strong params (`4f38e1b`)
- **factory:** cherry-pick security, performance, and test improvements from MiniMax factory (`96881c3`)
- **security+perf:** cherry-pick OpenClaw bot findings — IDOR, injection, N+1, indexes (`239f7c9`)
- preserve original_description in tasks (#615) (`4827acf`)
- **security:** sprint 1 — security hardening across 24 findings (`fe0be95`)
- **nightshift:** async fire-and-forget architecture with hook token auth (`35ae4d6`)
- **ci:** rubocop auto-fix, hook auth blank token, needs_follow_up default, memory search probe (`4e81196`)
- **ci:** resolve scan_js and scan_ruby failures (`afc6a9a`)
- interval_ms conversion (milliseconds not a method) + add status query methods (`ca3004a`)
- unify navbar icons across all pages (#566) (`de174e5`)
- correct GitHub URL to wolverin0/clawtrol (`6a5e531`)
- eager loading, navbar links, landing page redesign (#564) (`222a740`)
- **command:** avoid /command.json 503 by caching + configurable CLI timeout (`740fdbd`)
- **command:** make Command Center use OpenClaw CLI sessions + add tests (`e20f352`)
- /keys removes admin check (use login auth only) (`a2c39dd`)
- nightshift uses Rails layout (keeps navbar/sidebar) (`16580a4`)
- nightshift mobile — disable layout, fix checkboxes, increase bottom padding (`eb88619`)
- remove hardcoded OpenAI key from marketing_controller (`a7e32b4`)
- nightshift UI — inline Stimulus controller + disable scanline on mobile (`592aceb`)
- use gemini-3-flash-preview model for link processing (`3b758a6`)
- use Gemini CLI with OAuth instead of API key, gemini-2.5-flash model (`fda8b3d`)
- use Gemini 3 Flash for saved link processing (`7941afa`)
- register mobile-nav controller explicitly in Stimulus index (`af9800d`)
- remove authenticate_user! from NightshiftController (uses Authentication concern) (`958c0d1`)
- archived view for aggregator board shows all boards instead of only board_id=1 (`d6d3b42`)
- memory health = OK when gateway is reachable (`2b37889`)
- memory search health check uses /api/sessions instead of nonexistent /api/memory/search (`6ff23aa`)
- **mobile:** memory panel uses fixed positioning like notifications (`5ae0f71`)
- **mobile:** board tabs as dropdown select + memory panel as bottom sheet (`1b347eb`)
- **mobile:** board tabs scrollable + memory panel stays in viewport (`add67b4`)
- **views:** improve mobile responsiveness of board header (`b5a770b`)
- catch StandardError not Exception in bare rescue clauses (`a83c0da`)
- **security:** reject absolute paths in view_file, tighten allowed_dirs (#495) (`1660d85`)
- **security:** require authentication for FileViewerController (#493) (`de32dfb`)
- **security:** require auth for agent_log and session_health endpoints (#492) (`73f8538`)
- **security:** sanitize all Redcarpet markdown output to prevent XSS (#491) (`b3260b2`)
- serve binary files with proper content-type in view_file (`93c54c5`)
- openclaw-owned orchestration + follow-up approval + UI persona controls (`a8472b1`)
- live kanban refresh + persona controls + hook broadcasts (`9405393`)
- auto-pull circuit breaker + outcome runs + notification dedup/cap (`dd0d364`)
- auto-pull circuit breaker + outcome runs + notification dedup/cap (`15ca78e`)
- agent_complete sets assigned_to_agent=true so done validation works (`2a89875`)
- auto-link agent_session_id on task update and agent_complete (`0ec7b09`)
- **mobile:** horizontal scroll for board tabs instead of wrapping (`b279f2c`)
- restore Personas & Rosters icons in navbar (`ae52fc1`)
- agent_log finds archived .deleted transcript files (`378d5be`)
- search — replace Stimulus with plain HTML form (`a871e0e`)
- 3 UI issues — done counter, search hint, AI model options (`d9df7cd`)
- NEXT button full page reload — remove turbo_frame '_top' (`758ef7e`)
- permanent fixes for session linking + validation selection (`f2d2747`)
- system test broken by slash commands placeholder change (`7a68ff8`)
- delete drop zone missing <div> tag — raw HTML visible on board (`37e070a`)
- notifications mark_all_read broken — URL had .json suffix (`0b088b1`)
- z-index on all sub-modals (z-50 → z-[80]) — were trapped behind task panel (z-[60]/z-[70]) (`06a0017`)
- agent_activity + kanban_refresh controllers also had channels/index import bug (`1aea1bf`)
- Turbo not loading - channels/index import mismatch broke entire JS chain (`6162f51`)
- auto-open task uses setTimeout + better card selector (`2688c72`)
- auto-open task modal via turbo frame on ?task=ID redirect (`6800121`)
- redirect direct task visits to board with auto-open via ?task=ID (`97eaedc`)
- task show renders panel directly on direct visits, turbo_frame on AJAX (`47d7148`)
- render task show with full layout on direct visits (not just Turbo Frame) (`8823639`)
- explicitly register new Stimulus controllers (Propshaft digest cache bypass) (`cc692ec`)
- lock body scroll when fullscreen file viewer is open (`54e8ef0`)
- populate fullscreen content BEFORE moving to body (targets disconnect after move) (`7cc6d3e`)
- expand button - move fullscreen modal to body to escape transform containing block (`6568fb7`)
- rename 'No agent session' to 'Waiting for agent connection' + improve messaging (`250ecec`)
- auto-routing now matches ClawTrol prefix, defaults to ClawDeck board (`7706f20`)
- file viewer fetch approach + auto session linking (#121) (`cdb12e9`)
- set empty @columns in archived action for header partial (`533e510`)
- auto-done in web controller, done ordering, spinner indicator (`3d6ac4f`)
- hide preview on click, fix agent output fallback for completed tasks (`ddd2b63`)
- hide hover preview when context menu opens (`83b20a8`)
- context menu z-index above hover preview (`d9b62c9`)
- increase truncation limits in agent terminal panel (`a5cf4be`)
- use reorder() to override default_scope for correct column ordering (`b0b52f0`)
- increase truncation limits in agent activity panel (5000/1500/3000 chars) (`8ebdf03`)
- disable sorting in in_review/done columns, re-prepend on update for date order (`42becfd`)
- increase content limits, fix fallback regex, verify column ordering (`5096c10`)
- pin to terminal event dispatch to document instead of window (`50a2c6b`)
- hover preview uses fixed positioning to avoid overflow clipping (`ec14950`)
- hover preview uses modal, transcript fallback to description (`2586be5`)
- Show session continuation UI even without session_key (`0efa03d`)
- Agent modal now uses same centered pattern as followup modal (`50f1256`)
- Mobile modal now uses proper bottom sheet pattern (`a6a479f`)
- Mobile agent modal now slides up correctly (`8d52e8a`)
- Sort in_review/done columns by most recent first (`43e5bde`)
- Generate AI suggestion now uses session auth instead of API token (`0ee8cc2`)
- NEXT button persists after drag between columns (`6b4db1b`)
- Use link_to instead of button_to for NEXT button (`190d84b`)
- Follow-up modal improvements (`f34355e`)
- NEXT button + async follow-up modal + model selector (`4e727f2`)
- Move action buttons outside link wrapper (`c6c477c`)
- Disable GLM thinking mode for proper content response (`2a93b83`)
- task drag-and-drop not persisting to database (`519a035`)
- guard against nil current_user in task card broadcast (`a27a195`)
### 📚 Documentation

- **audit:** vibe-code-audit v2.1 + codebase maps (2026-04-17) (`2a26fef`)
- update CHANGELOG.md [skip ci] (`1f5a94b`)
- update CHANGELOG.md [skip ci] (`86f6484`)
- update CHANGELOG.md [skip ci] (`8c32ad4`)
- update CHANGELOG.md [skip ci] (`4d2258c`)
- rewrite features section — ZeroBitch fleet, factory, nightshift, personas, roadmaps, swarm, saved links all properly described (`c23dd9a`)
- update CHANGELOG.md [skip ci] (`f3ae1a5`)
- clean README with collapsible sections, fix skill IP/name, remove internal files (`8fadd8e`)
- update CHANGELOG.md [skip ci] (`13ec09e`)
- update CHANGELOG.md [skip ci] (`5787a44`)
- update CHANGELOG to 2026.3.0 and README feature list (`a999ce3`)
- [factory] log dependency scan perf tweak (`891eca8`)
- [factory] log api token scope tweak (`8aed1b3`)
- [factory] log validation safety concern (`91773ce`)
- [factory] log task dependency validation guard (`2ae08b0`)
- [factory] log api token expiry guard (`474d12a`)
- [factory] log model limit timing tweak (`a7e5fad`)
- [factory] log validation prefix test (`d8d8183`)
- [factory] log code quality cleanup (`e2d416d`)
- [factory] log dead route scanner security hardening (`d447b08`)
- [factory] log dead route turbo-frame mismatch fix (`5e8e8c8`)
- [factory] log promotion gate fail-fast cycle (`df13936`)
- update CHANGELOG.md [skip ci] (`9fe5b78`)
- [factory] log security cache-header hardening cycle (`6b7f426`)
- [factory] log dead route scanner GET matching fix (`de5efe5`)
- update CHANGELOG.md [skip ci] (`304a18e`)
- update CHANGELOG.md [skip ci] (`b570fef`)
- update CHANGELOG.md [skip ci] (`2bb2cca`)
- update CHANGELOG.md [skip ci] (`df50665`)
- [factory] log workflow engine logger consistency cycle (`ceb3f0e`)
- update CHANGELOG.md [skip ci] (`4e23afa`)
- update README + onboarding + integration for P0 data contract and session linking (`7ac9a3a`)
- [factory] log security expression guard cycle (`9769a36`)
- [factory] log mission control cache hardening cycle (`4f3917d`)
- [factory] log empty-response scanner hardening (`7ec518d`)
- [factory] update improvement log with mission control health dashboard (`999b5b2`)
- update CHANGELOG.md [skip ci] (`d0191e5`)
- update CHANGELOG.md [skip ci] (`6712268`)
- update CHANGELOG.md [skip ci] (`5234b40`)
- update CHANGELOG.md [skip ci] (`37e544a`)
- update CHANGELOG.md [skip ci] (`a044e4e`)
- **report:** add 2026-02-20 big release commit/push report (`0e06cc6`)
- update CHANGELOG.md [skip ci] (`70dfb29`)
- update CHANGELOG.md [skip ci] (`e1f9c88`)
- add ZeroBitch cover image to README (`2c9454d`)
- update CHANGELOG.md [skip ci] (`9d41170`)
- update CHANGELOG.md [skip ci] (`81bde00`)
- update CHANGELOG.md [skip ci] (`45f38a7`)
- update CHANGELOG.md [skip ci] (`c68c4c7`)
- update CHANGELOG.md [skip ci] (`d741cff`)
- update CHANGELOG.md [skip ci] (`b978993`)
- update CHANGELOG.md [skip ci] (`27b024d`)
- update CHANGELOG.md [skip ci] (`3b3fb2a`)
- update CHANGELOG.md [skip ci] (`a3e8bf1`)
- update CHANGELOG.md [skip ci] (`2e78f03`)
- update CHANGELOG.md [skip ci] (`15d30a6`)
- add complete feature diagnostic with intent analysis for 30 features (`6954cf5`)
- update CHANGELOG.md [skip ci] (`c83354c`)
- **readme:** explain what ZeroBitch is — swarm orchestration for ZeroClaw instances (`c868e1e`)
- update CHANGELOG.md [skip ci] (`d36c341`)
- update README + CHANGELOG for v0.9.0 release (`26b95c2`)
- add OpenClaw onboarding and settings self-heal prompts (`2c8f2bb`)
- update CHANGELOG.md [skip ci] (`bac4c30`)
- update CHANGELOG.md [skip ci] (`274063c`)
- update CHANGELOG.md [skip ci] (`69ca3f9`)
- update CHANGELOG.md [skip ci] (`0ce9586`)
- update CHANGELOG.md [skip ci] (`5a7c9af`)
- update CHANGELOG.md [skip ci] (`0ad21ef`)
- sync FACTORY_BACKLOG checkmarks with implemented factory commits (`3452c9a`)
- update CHANGELOG.md [skip ci] (`0852c9a`)
- update CHANGELOG.md [skip ci] (`f125109`)
- add wave 3 features to README — pipeline, swarm, config hub, factory, API endpoints (`ee3c61a`)
- update CHANGELOG.md [skip ci] (`482d574`)
- update CHANGELOG.md [skip ci] (`35bd7fb`)
- add swarm API reference + changelog entries for favorites, pipeline stepper, bugfixes (`9b4d637`)
- update CHANGELOG.md [skip ci] (`8a29aa6`)
- update CHANGELOG.md [skip ci] (`6e8c1bc`)
- update CHANGELOG.md [skip ci] (`c1eadcf`)
- update CHANGELOG.md [skip ci] (`421fc81`)
- update CHANGELOG.md [skip ci] (`f71d6ba`)
- update CHANGELOG.md [skip ci] (`19d7e24`)
- update CHANGELOG.md [skip ci] (`7531db9`)
- update CHANGELOG.md [skip ci] (`8ebd520`)
- update CHANGELOG.md [skip ci] (`52699e8`)
- update CHANGELOG.md [skip ci] (`c9ab7b2`)
- update CHANGELOG.md [skip ci] (`e6733a9`)
- update README with Telegram notifications setup and Nightshift execution backend (`b704105`)
- update README with Nightshift, Link Inbox, mobile nav, search features (`1ec651e`)
- update README with Nightbeat, auto-save pipeline, done validation, transcript recovery (`a2d18e7`)
- add AI-assisted installation prompt (`a0e904b`)
- comprehensive README update — Feb 5 sprint (`c437f55`)
- Rebrand to ClawTrol with comprehensive feature documentation (`3b3cecf`)
- comprehensive changelog for PR #16 (`47b01bd`)
- Add changelog draft for v0.2.0 (`05f8c55`)
- rewrite agent integration for v2 API (`38fd3fa`)
### ♻️  Refactoring

- remove dead pipeline code from Task, AutoRunner, WebhookService (`e14a74b`)
- [factory] make api token by_user chainable (`b2d5392`)
- [factory] share validation command safety (`e991c1a`)
- [factory] architecture preload roadmap sync links and tasks (`222bfff`)
- [factory] remove duplicate strict_loading (`15a313b`)
- [factory] architecture refactor daily executive digest service scope (`409519c`)
- [factory] remove unused legacy nav_icons partial (`c2fa729`)
- [factory] extract dead route scan result builders (`ff4bceb`)
- [factory] use injected logger in workflow engine (`7c379ee`)
- [factory] unify last-board nav path resolution (`0728ce5`)
- [factory] code quality centralize promotion gate checks catalog (`3717bb5`)
- [factory] add promotion gate service for cherry-pick verify (`7ad87f6`)
- [factory] code quality: DRY default work branch fallback (`ff0083b`)
- [factory] architecture: extract mission control health snapshot service (`24988bd`)
- [factory] Unified Navbar Registry (desktop + mobile parity) (`1829c58`)
- [factory] extract BehavioralInterventionUpdaterService (`013a9bd`)
- remove pipeline_assist mode — openclaw_only is the sole orchestration path (`327e6af`)
- **ux:** sprint 5 — design consistency, controller consolidation, themes, accessibility (`f31ac42`)
- extract Task concerns from god object (#503) (`d865974`)
### ⚡ Performance

- [factory] avoid shift in dependency scan (`c7844e3`)
- [factory] reuse time in model limit reset (`7226dff`)
- [factory] reduce digest query churn (`644ec6b`)
- [factory] fail-fast promotion gate checks (`89b714a`)
- [factory] dedupe sessions explorer task-link keys (`f92fb5d`)
- [factory] cache mission control health snapshot (`b725955`)
- [factory] dedupe dead route paths in single pass (`051f85f`)
- [factory] Performance: use Open3 chdir for promotion checks (`2bfea0b`)
- [factory] skip wildcard routes in dead route scanner (`f10d40a`)
- **kanban:** lazy-load task context menu and agent modal (`72d86f3`)
- **kanban:** add task_dependencies preload, fix status-change blocking ids, simplify cache key (`3dd0564`)
- **kanban:** preload dependency data, frame board switches, cache task cards (`e138ad5`)
- **backend:** sprint 2 — race conditions, indexes, pagination, N+1 fixes (`fe82233`)
### 🧪 Tests

- add unit tests for BoardRoadmap checked_items and progress_summary (#692) (`84a8e36`)
- add system tests for roadmap modal, progress, and context menu (#692) (`93068df`)
- [factory] cover validation command prefix (`c3f83a0`)
- [factory] cover sub-agent output contract normalization (`a2be4a3`)
- [factory] cover git worktree marker in promotion gate (`44be431`)
- [factory] cover navigation helper active-state branches (`a1fa6e4`)
- [factory] add primary_navigation board-link regression tests (`6ed088e`)
- [factory] add mission control cache ttl regression (`8dc0304`)
- [factory] add promotion gate timeout/path regression tests (`e34eb0e`)
- [factory] Testing: add factory engine init fallback regression (`d2ca12b`)
- [factory] add ModelCatalogService coverage (`2ed9227`)
- [factory] cover dead route scanner behavior (`a42b7f7`)
- [factory] fix missing assertions in job and model tests (`eb2bdad`)
- stabilize board system login setup for deterministic selenium runs (`270da66`)
- add controller + integration tests (#506) (`b9adaa1`)
### 👷 CI

### 💄 Style

### 📦 Build

### 🔧 Chores

- merge factory/minimax-auto — Mission Control health dashboard, promotion gate, dead route scanner, API token expiry, roadmap sync (`b0d2525`)
- remove codemap feature, assets, and monitor endpoints (`6e3c8fd`)
- remove unused large binary artifacts from repo (`8d5a6aa`)
- cherry-pick minimax factory improvements from clawtrolplayground (`e2d7199`)
- split backlog — 72 done items to FACTORY_BACKLOG_DONE.md, 28 pending remain (`b73c21b`)
- update schema after project_path migration (`992b1f4`)
- **nightshift:** schedule timeout sweeper job hourly (`9ba397e`)
- **deps:** add dependabot config for bundler and npm_and_yarn (`f2e37f9`)
- add dependabot config for bundler npm and actions (`e867186`)
- remove git-rewrite artifacts (`51a954e`)
- prompt outcome contract in auto-pull (`9bac803`)
- normalize file modes (`fee6025`)
- pending UI changes (filters, cards, badges, panel) (`529a381`)
- **deps:** Update gems (importmap-rails, solid_cache, solid_queue, tailwindcss-rails) (`a56a2fb`)
- **deps:** bump pg from 1.6.2 to 1.6.3 (#11) (`9714073`)
- **deps-dev:** bump brakeman from 7.1.0 to 8.0.2 (#9) (`4dcca82`)
- **deps-dev:** bump bundler-audit from 0.9.2 to 0.9.3 (#6) (`faaf6e1`)
- **deps:** bump puma from 7.1.0 to 7.2.0 (#4) (`8c30b61`)
- **deps:** bump actions/upload-artifact from 4 to 6 (#3) (`9e3e869`)
- **deps:** bump actions/cache from 4 to 5 (#2) (`8dceab3`)
- **deps:** bump actions/checkout from 5 to 6 (#1) (`47897eb`)
- remove debug console.log statements, production ready (`d71c50b`)
- migrate domain from app.clawdeck.io to clawdeck.io (`2e270fd`)

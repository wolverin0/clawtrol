<% content_for :breadcrumb_standalone do %>
  Tokens
<% end %>

<div data-controller="tokens" class="min-h-screen bg-background text-text p-6 space-y-6">
  <div class="flex items-center justify-between pb-4 border-b border-white/5">
    <h1 class="text-2xl font-bold tracking-tight flex items-center gap-3 text-white">
      🪙 TOKEN USAGE
    </h1>

    <div data-tokens-target="status">
      <span class="inline-flex items-center gap-2 px-2 py-1 rounded-full bg-gray-500/10 text-gray-400 text-xs font-medium border border-gray-500/20">
        <span class="w-1.5 h-1.5 rounded-full bg-gray-500"></span>
        LOADING...
      </span>
    </div>
  </div>

  <div data-tokens-target="error" class="hidden bg-red-500/10 border border-red-500/20 text-red-200 rounded-lg p-4 text-sm"></div>

  <div class="grid grid-cols-1 lg:grid-cols-4 gap-6">
    <div class="lg:col-span-3 space-y-4">
      <h2 class="text-sm font-semibold text-muted uppercase tracking-wider flex items-center gap-2">
        Active sessions (last 24h)
        <span class="w-full h-px bg-white/5"></span>
      </h2>

      <div data-tokens-target="empty" class="hidden flex flex-col items-center justify-center py-20 border border-dashed border-white/10 rounded-lg text-muted">
        <div class="text-4xl mb-4 opacity-50">🫥</div>
        <p>No sessions found</p>
      </div>

      <div data-tokens-target="list" class="space-y-2">
        <div class="bg-card border border-border rounded-lg p-4 text-xs text-muted italic">Fetching sessions…</div>
      </div>
    </div>

    <div class="space-y-6">
      <div class="bg-card border border-border rounded-lg p-4">
        <h3 class="text-xs font-bold text-muted uppercase mb-3">Summary</h3>
        <div data-tokens-target="summary" class="space-y-2">
          <div class="text-xs text-muted italic">Loading…</div>
        </div>
      </div>

      <div class="bg-card border border-border rounded-lg p-4">
        <h3 class="text-xs font-bold text-muted uppercase mb-3">Notes</h3>
        <div class="text-xs text-muted space-y-2">
          <p>• Tap a row to copy session id</p>
          <p>• Tokens are from <span class="font-mono text-text">openclaw sessions</span></p>
        </div>
      </div>
    </div>
  </div>
</div>

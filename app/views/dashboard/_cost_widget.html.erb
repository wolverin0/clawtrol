<% if @gateway_cost.present? && !@gateway_cost["error"] %>
  <div class="bg-bg-surface rounded-xl border border-border p-4">
    <h3 class="text-sm font-medium text-content-muted mb-3 flex items-center gap-2">
      <span>💰</span> Token Usage
    </h3>
    <div class="grid grid-cols-2 gap-3">
      <div>
        <div class="text-2xl font-bold text-content">
          <%= number_with_delimiter(@gateway_cost["totalTokens"] || @gateway_cost["total_tokens"] || 0) %>
        </div>
        <div class="text-xs text-content-muted">Total Tokens</div>
      </div>
      <div>
        <div class="text-2xl font-bold text-emerald-400">
          $<%= number_with_precision((@gateway_cost["totalCost"] || @gateway_cost["total_cost"] || 0).to_f, precision: 2) %>
        </div>
        <div class="text-xs text-content-muted">Total Cost</div>
      </div>
    </div>
  </div>
<% end %>

<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "clawdeck" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1,maximum-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-status-bar-style" content="default">
    <meta name="apple-mobile-web-app-title" content="ClawDeck">
    <meta name="application-name" content="ClawDeck">
    <meta name="mobile-web-app-capable" content="yes">
    <%# Theme colors for Safari and other browsers %>
    <meta name="theme-color" content="#f5f5f4" media="(prefers-color-scheme: light)">
    <meta name="theme-color" content="#0c0a09" media="(prefers-color-scheme: dark)">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%# Fonts %>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Nunito:ital,wght@0,200..1000;1,200..1000&display=swap" rel="stylesheet">
    <%= yield :head %>
    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>
    <%# Favicons %>
    <link rel="icon" type="image/x-icon" href="/favicon.ico">
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png">
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png">
    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <%= render "application/plausible_analytics" %>
  </head>
  <body class="h-screen overflow-hidden bg-stone-100 dark:bg-stone-950 p-2" data-controller="turbo-progress">
    <%# Main content panel - full viewport with rounded corners, no sidebar %>
    <div class="h-[calc(100vh-1rem)] flex flex-col shadow-sm bg-stone-50 dark:bg-stone-900 rounded-3xl overflow-hidden">
      <%# Simple navbar for auth pages %>
      <nav class="relative flex-shrink-0 z-50 flex items-center justify-between px-6 py-4">
        <%# Logo on the left %>
        <div class="flex items-center flex-shrink-0">
          <%= link_to root_path, class: "flex items-center" do %>
            <%= image_tag "clawdecklogo.svg", alt: "ClawDeck Logo", class: "h-6 w-auto" %>
          <% end %>
          <%# Loading spinner %>
          <div data-turbo-progress-target="spinner" class="opacity-0 p-2 transition-opacity pointer-events-none">
            <svg class="animate-spin w-4 h-4 text-stone-400" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24">
              <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle>
              <path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4zm2 5.291A7.962 7.962 0 014 12H0c0 3.042 1.135 5.824 3 7.938l3-2.647z"></path>
            </svg>
          </div>
        </div>
        <%# Center: Flash messages %>
        <div class="absolute left-1/2 top-1/2 -translate-x-1/2 -translate-y-1/2">
          <% if notice.present? || alert.present? %>
            <div class="flex flex-col items-center gap-1.5 pointer-events-none">
              <% if notice.present? %>
                <div data-controller="flash" class="py-1.5 px-3 bg-lime-50 dark:bg-lime-900/30 text-lime-600 dark:text-lime-400 text-xs font-medium rounded-md transition-opacity duration-500 shadow-sm whitespace-nowrap" role="alert">
                  <%= notice %>
                </div>
              <% end %>
              <% if alert.present? %>
                <div data-controller="flash" class="py-1.5 px-3 bg-red-50 dark:bg-red-900/30 text-red-600 dark:text-red-400 text-xs font-medium rounded-md transition-opacity duration-500 shadow-sm whitespace-nowrap" role="alert">
                  <%= alert %>
                </div>
              <% end %>
            </div>
          <% end %>
        </div>
        <%# Empty right side for balance %>
        <div class="flex items-center gap-1 flex-shrink-0">
        </div>
      </nav>

      <div class="flex-1 overflow-y-auto px-4 mb-4 overscroll-contain [-webkit-overflow-scrolling:touch]">
        <main class="container max-w-lg mx-auto pb-8 text-stone-600 dark:text-purple-100 font-normal text-sm">
          <%= yield %>
        </main>
      </div>
    </div>
  </body>
</html>

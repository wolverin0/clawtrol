<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "clawdeck" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1,maximum-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="apple-mobile-web-app-status-bar-style" content="default">
    <meta name="apple-mobile-web-app-title" content="ClawDeck">
    <meta name="application-name" content="ClawDeck">
    <meta name="mobile-web-app-capable" content="yes">
    <%# Theme colors for Safari and other browsers %>
    <meta name="theme-color" content="#050810">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%# Fonts - Clash Display + Satoshi %>
    <link href="https://api.fontshare.com/v2/css?f[]=clash-display@700,600,500&f[]=satoshi@400,500,700&display=swap" rel="stylesheet">
    <%= yield :head %>
    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>
    <%# Favicons %>
    <link rel="icon" type="image/x-icon" href="/favicon.ico">
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png">
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png">
    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <%= render "application/plausible_analytics" %>
  </head>
  <body class="h-screen overflow-hidden bg-bg-base font-body" data-controller="turbo-progress keyboard-shortcuts">
    <% if @board_page %>
      <%# Board page - full screen, no wrapper %>
      <div class="h-screen flex flex-col bg-bg-base">
        <main class="h-full text-content-secondary font-normal text-sm pb-0" data-agent-terminal-aware>
          <%= yield %>
        </main>
      </div>

      <%# Mobile Bottom Navigation - only visible on mobile %>
      <nav class="fixed bottom-0 left-0 right-0 bg-bg-elevated border-t border-border flex justify-around py-2 md:hidden z-50 safe-area-bottom">
        <%= link_to dashboard_path,
            class: "flex flex-col items-center gap-0.5 text-xs px-3 py-1 rounded-lg transition-colors #{'text-content bg-bg-surface' if controller_name == 'dashboard'}  #{'text-content-muted hover:text-content' unless controller_name == 'dashboard'}" do %>
          <span class="text-lg">📊</span>
          <span>Dashboard</span>
        <% end %>

        <%= link_to (@board ? board_path(@board) : boards_path),
            class: "flex flex-col items-center gap-0.5 text-xs px-3 py-1 rounded-lg transition-colors #{'text-content bg-bg-surface' if controller_name == 'boards'}  #{'text-content-muted hover:text-content' unless controller_name == 'boards'}" do %>
          <span class="text-lg">📋</span>
          <span>Board</span>
        <% end %>

        <button type="button"
                data-action="click->mobile-columns#showTerminal"
                class="flex flex-col items-center gap-0.5 text-xs text-content-muted hover:text-content px-3 py-1 rounded-lg transition-colors cursor-pointer">
          <span class="text-lg">🖥</span>
          <span>Terminal</span>
        </button>

        <%= link_to settings_path,
            class: "flex flex-col items-center gap-0.5 text-xs px-3 py-1 rounded-lg transition-colors #{'text-content bg-bg-surface' if controller_name == 'profiles'}  #{'text-content-muted hover:text-content' unless controller_name == 'profiles'}" do %>
          <span class="text-lg">⚙️</span>
          <span>Settings</span>
        <% end %>
      </nav>

      <%# Agent Terminal Panel - only on board pages %>
      <%= render "layouts/agent_terminal" %>
    <% else %>
      <%# Other pages - centered content with navbar %>
      <div class="h-screen flex flex-col bg-bg-base">
        <%= render "application/navbar" %>
        <div class="flex-1 overflow-y-auto px-4 pb-8 overscroll-contain [-webkit-overflow-scrolling:touch]">
          <main class="container max-w-lg mx-auto text-content-secondary font-normal text-sm">
            <%= yield %>
          </main>
        </div>
      </div>
    <% end %>
  </body>
</html>

# Read about fixtures at https://api.rubyonrails.org/classes/ActiveRecord/FixtureSet.html

one:
  task: two
  depends_on: one

# Note: self-dependencies (task: X, depends_on: X) were previously here — those are invalid.
# A task cannot depend on itself.
